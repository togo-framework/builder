---
description: "Reserved slot. The wizard may emit a project-specific rule here; until then this file is intentionally inert."
globs: "*"
alwaysApply: false
---

# Rule 19: Reserved

**Reserved for a wizard-generated project rule — this slot exists so the numbering has no silent gaps.**

Slot 19 is one of three (17, 18, 19) held for rules the builder wizard generates from the
project's own answers ({{surfaces}}, {{env_matrix}}, {{promotion_mode}}) or that
{{project_name}} adds for itself. Blueprint version: {{blueprint_version}}. Do not renumber the
bands to fill it; write the rule here.
